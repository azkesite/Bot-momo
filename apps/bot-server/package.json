{
  "name": "bot-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@bot-momo/config": "workspace:*",
    "@bot-momo/core": "workspace:*"
  },
  "scripts": {
    "dev": "node --enable-source-maps ./src/main.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
