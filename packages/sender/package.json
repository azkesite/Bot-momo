{
  "name": "@bot-momo/sender",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "dependencies": {
    "@bot-momo/core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
